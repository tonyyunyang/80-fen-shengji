# 一起做一局更好的八十分

[English](CONTRIBUTING.en.md) · [回到项目](README.md)

欢迎贡献，也欢迎先玩几局。你可以修复交互、补规则例子、改翻译、画牌面、改善无障碍，或研究更好的搭档策略。

**请通过 Pull Request 提交改进。维护者会不定期查看与审核，回复可能需要一些时间。** 这是一项业余项目，没有固定的评审周期。小而完整的 PR 通常更容易理解。

## 第一个 PR

1. Fork 仓库，创建一个描述清楚的分支。
2. 安装 Node.js 22+，运行下面的命令。
3. 完成一项具体改进，补充相关验证与文档。
4. 向 `main` 提交 PR，说明问题、改动后的行为，以及如何检查。

```sh
npm ci --ignore-scripts
npm start
# 在另一个终端执行验证
npm test
npm run check
```

本地地址为 http://127.0.0.1:5173。无需 API key，也不需要构建步骤。常规测试全部离线。界面变化请附截图或短 GIF；规则变化请附主牌、手牌、出牌与期望结果的具体例子。

## 什么样的改进最有帮助

- **交互与无障碍：** 稳定的悬停、键盘操作、焦点、对比度、窗口缩放。
- **规则：** 能复现的边界牌例。八十分有很多地方规则，请明确本项目规则和你熟悉的规则之间的差异。
- **翻译与说明：** 自然的中文或英文，尤其是牌戏术语和新手解释。
- **美术：** 保持当前像素牌桌与清晰花色；注明素材来源与许可。
- **AI：** 提高团队胜负与得分表现，提供可比较的结果，单独报告超时与陪练接管。

较大的设计或规则方向，先开一个 Issue 讨论会节省彼此时间。小修正可直接提交 PR。AI 辅助贡献同样欢迎；请像其他代码一样理解、检查并验证它。

## 几条共同遵守的边界

- 只有 `public/index.html` 这一张正式牌桌，不新增 hand-lab、旧桌面或演示入口。
- `src/game.js` 和 `src/rules.js` 保持确定性，不调用网络或厂商策略。
- `vendor/peilian/reference-core.cjs` 是保留的上游副本；通过适配层使用，不直接修改。
- 模型只看自己的观察，不看其他手牌、洗牌种子、未知底牌或练习答案。
- 保留共享 12 秒决策时限、失败接管、持续发牌与私密亮主机制。
- PR 不包含 key、会话存档、原始牌谱或私人请求日志。示例请使用虚构数据。

这些边界保护了公平性，也让界面、规则和模型可以分别改进。[架构说明](docs/architecture.md)与[模型协议](docs/player-protocol.md)有更具体的接口。

## 验证到什么程度

运行 `npm test` 和 `npm run check`。界面改动还应实际测试手动出牌、混合座位、下一局／重开、较窄窗口与浏览器控制台；不要为了录制素材调用付费模型。

需要真实 API 评测时，使用自己的凭据，主动启用并给请求数、总时长与输出量设限。不要默认使用别人保存的 key。比较团队胜负，不以是否复制陪练的出牌来评分。[评测说明](docs/research/ai-evaluation.md)

## 评审与许可

PR 描述不需要很长：说明具体问题、最终行为和验证结果即可。若代码或素材来自其他项目，保留来源、许可与必要通知。除非另行明确约定，提交到本项目的贡献按 [Apache-2.0](LICENSE) 许可。

请尊重其他贡献者，遵守[社区约定](CODE_OF_CONDUCT.md)。安全问题请通过[私密报告渠道](SECURITY.md)反馈。

谢谢你让这张牌桌更好。祝玩得开心！
